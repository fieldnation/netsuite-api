{
  "name": "netsuite-api",
  "version": "1.0.0",
  "description": "NetSuite API",
  "main": "lib/netsuite.js",
  "scripts": {
    "test": "mocha --recursive",
    "test-ci": "mocha --recursive --reporter mocha-junit-reporter --check-leaks --bail",
    "test-cov": "istanbul cover --report html node_modules/mocha/bin/_mocha -- --recursive --check-leaks --reporter dot",
    "lint": "eslint lib"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@bitbucket.org/fieldnation/netsuite-api.git"
  },
  "keywords": [
    "NetSuite",
    "API"
  ],
  "author": "Field Nation",
  "license": "UNLICENSED",
  "homepage": "https://bitbucket.org/fieldnation/netsuite-api#readme",
  "dependencies": {
    "soap": "^0.17.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^6.8.0",
    "istanbul": "^0.4.5",
    "libxmljs": "^0.18.0",
    "mocha": "^3.1.0",
    "mocha-junit-reporter": "^1.12.0"
  }
}
